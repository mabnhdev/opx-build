# this is your configuration file for pbuilder.
# the file in /usr/share/pbuilder/pbuilderrc is the default template.
# /etc/pbuilderrc is the one meant for overwriting defaults in
# the default template
#
# read pbuilderrc.5 document for notes on specific options.
MIRRORSITE=http://httpredir.debian.org/debian
HOOKDIR=/var/cache/pbuilder/hook.d
OTHERMIRROR="deb [trusted=yes] file:///var/cache/pbuilder/result ./|deb [trusted=yes] http://bldecos-1.extremenetworks.com/apt/opx ecos main contrib non-free"
BINDMOUNTS=/var/cache/pbuilder/result
# preinstall most of the packages that the build needs in order to speed it up
EXTRAPACKAGES="apt-utils dh-autoreconf debhelper libxml2-dev python-dev exuberant-ctags fakeroot dh-systemd dh-python libhiredis-dev libsystemd-dev"
